[tool.poetry]
name = "shock_pods"
version = "0.0.1"
description = "A set of functions for news topic modelling and embedding"
authors = [
  "edazizovv",
]
license = "MIT"
readme = "README.md"
packages = [{include = "shock_pods"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"